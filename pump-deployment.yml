apiVersion: apps/v1
kind: Deployment
metadata:
  name: refinery-pump
spec:
  replicas: 3
  selector:
    matchLabels:
      app: oil-flow
  template:
    metadata:
      labels:
        app: oil-flow
    spec:
      containers:
      - name: pump-instance
        image: nginx:latest